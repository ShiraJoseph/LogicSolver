import {GridSeed} from '../app/types/grid.model';
import {MOCK_GRID_SEED} from '../app/mocks/grid.mock';

/** The development settings. */
export const environment: {gridSeed: GridSeed} = {
  gridSeed: MOCK_GRID_SEED
};
